# Укрепление существующей ERP

## Этап 1 — загрузка, вход и черновики (5 сентября 2026)

Изменения сделаны локально. Новые бизнес-модули, SQL-миграции и изменения финансовых формул в этот этап не входят. Публикация и работа с боевыми данными не выполнялись.

### Что исправлено

- Основной экран монтируется только после загрузки действующего профиля. Ошибка, отсутствующий профиль и неизвестная роль не открывают рабочие разделы. Предусмотрена повторная проверка. Запоздавший ответ старой сессии не подменяет новую.
- Кнопки обновления явно вызывают загрузку без события клика в аргументах.
- Ошибка одной таблицы не обнуляет её последние загруженные значения и не отменяет успешные ответы остальных источников. Старый параллельный запрос не перезаписывает более новый.
- Заявки и расход препаратов применяются вместе, только после успешной загрузки обеих таблиц. Соединение выполняется через индекс по идентификатору заявки.
- Постраничная загрузка заявок, расхода и движения денег использует стабильную сортировку. Ошибка следующей страницы и превышение защитного лимита не выдаются за полную историю. Это не транзакционный снимок базы: параллельные изменения на сервере всё ещё требуют отдельной проверки.
- Предупреждение о неполных или устаревших данных видно всем ролям; технические детали — только администратору. Время полного обновления не меняется после неудачной попытки. Зелёное сообщение об отсутствии проблем не подменяет ошибку загрузки.
- Офлайн-снимок разделён по пользователю, роли, филиалу и настройкам доступа. Старый общий снимок v4 больше не используется. Новый снимок не включает зарплату и начальные денежные остатки профилей; неполный кэш до 400 заявок явно обозначается. Несохранённые пользовательские черновики не удалялись.
- Черновик отчёта сохраняет оборудование, осмотры точек, причину и пояснение скидки, срок долга и остальные поля. Черновик новой заявки больше не зависит от таймера, который мог не сработать перед закрытием.
- Недоступность или переполнение локального хранилища показываются в форме; интерфейс не заявляет, что офлайн-черновик сохранён, если запись не удалась.
- Формы заявки и отчёта выходят из состояния отправки после исключения. Повторное нажатие, редактирование и закрытие во время отправки блокируются. Удаление черновика требует явного `true` от обработчика сохранения. Неопределённый сетевой результат сопровождается предупреждением проверить заявку перед повторной отправкой.
- Убрано дублирующее свойство `priceList` в настройках.

### Проверки

Добавлены проверки на уровне функций, React DOM и основного экрана:

- отказ и повтор загрузки, сохранение старых заявок, согласованность заявок и расхода;
- гонки запросов и сессий, ожидание профиля, отключённый и неизвестный пользователь;
- восстановление черновика, quota error, исключение при отправке, двойное нажатие;
- предупреждения исполнителю и невозможность восстановления чужого/общего офлайн-снимка.

Команды: `npm test`, `npm run lint`, `npm run build`, `git diff --check`. Добавлен `jsdom` как dev-зависимость для тестов интерфейса. Граф проекта обновлён через `graphify update .`; SQL не индексируется без `tree_sitter_sql`.

Итог проверки: **305 тестов пройдено (29 новых)**; линтер и проверка diff — без ошибок; production-сборка успешна, предупреждение о размере стартового пакета остаётся.

### Границы и следующие приоритеты

1. **Цельное сохранение отчёта.** `submitReport` всё ещё вызывает несколько RPC/записей последовательно; часть дополнительных записей может завершиться ошибкой после сохранения основной части. В том числе существующий обработчик пока способен вернуть `true` после такой частичной ошибки. Нужна серверная транзакция, идемпотентный ключ и тесты отказа между шагами. Исходники действующих `submit_report`, `save_report_extras` и Edge Function `admin-users` отсутствуют в репозитории; нельзя считать этот риск закрытым по клиентским тестам.
2. **Права и филиалы.** Клиентская проверка профиля не заменяет RLS. Проверить действующие политики/функции в тестовой базе и наличие `profiles.branch_id`, которое теперь читает вход. При недоступной проверке профиля свежий запуск закрыт, в том числе полностью офлайн; уже открытый рабочий экран сохраняет последние данные. Полноценный безопасный офлайн-вход — отдельная задача.
3. **Очередь офлайн-действий и старые черновики.** Они пока используют существующие ключи. Для смены пользователя на общем устройстве нужны отдельная миграция владельца, сохранение неотправленных данных и безопасный конфликт-резолвинг. Разделение снимка не является полной изоляцией всего localStorage.
4. **Полнота остальных источников и журналов.** У ряда таблиц остаются серверные ограничения выборки; ленивые журналы требуют обработки ошибок и повторов. Требуются сверки складских остатков, кассы, переводов и долгов на реальных контрольных данных.
5. **Зависимости.** Установка dev-зависимости сообщила о 7 уязвимостях дерева зависимостей; их состав и доступность исправлений нужно проверить отдельно. Принудительное обновление с breaking changes не выполнялось.

Проверки с настоящей авторизованной сессией и серверными отказами в тестовой Supabase-базе не выполнялись. Это завершённый первый этап укрепления, а не подтверждение готовности всей ERP на «10 из 10».

## Этап 2 — скорость запуска и документы (7 сентября 2026)

- ExcelJS и PDFMake удалены из стартового пакета. Excel загружается при выгрузке, PDFMake — при создании акта или сертификата.
- Стартовый JavaScript уменьшен с 4,01 МБ до 0,98 МБ; gzip — с 1,60 МБ до 0,27 МБ (примерно на 83%). Отложенные части: Excel 0,94 МБ и PDF 2,08 МБ.
- Первый сформированный документ теперь получает только что загруженные печать и подпись. Раньше React ещё не успевал обновить `settings`, поэтому первый документ мог создаваться без изображений.
- Повторное нажатие на Excel во время подготовки файла заблокировано, кнопка показывает состояние загрузки.
- Первый Excel после открытия приложения использует данные журналов, возвращённые самим запросом, а не ещё не обновившееся состояние React. При ошибке журнала или корзины неполный файл не скачивается как будто он полный.
- Ошибки динамической загрузки и формирования документов показываются пользователю и попадают в журнал клиентских ошибок.

## Этап 3 — цельное сохранение отчёта (7 сентября 2026)

- Отчёт, оплата, расход препаратов, обход точек, концентрация, оборудование, долг и причина скидки теперь отправляются одним пакетом в `submit_report_atomic`.
- Сервер сохраняет пакет в одной транзакции: ошибка любой части откатывает весь отчёт, поэтому частично завершённая заявка больше не считается успехом.
- Для каждой попытки создаётся `request_id`. Он хранится в локальном черновике и повторно используется после обрыва связи, поэтому принятый сервером запрос не создаётся второй раз.
- Сервер повторно проверяет, что отчёт сдаёт назначенный исполнитель, администратор или сотрудник с правом редактировать заявки. Дата обхода берётся из самой заявки, а не из данных браузера.
- Если миграция ещё не применена, интерфейс блокирует старый небезопасный путь и прямо называет нужный SQL-файл; отчёт при этом не изменяется.
- Добавлены unit- и React DOM-проверки нормализации общего пакета, UUID повтора и сохранения одного `request_id` после неопределённого результата и повторного открытия формы.

Проверки: **314 тестов пройдено**; линтер и `git diff --check` — без ошибок; production-сборка успешна. Для окончательной серверной проверки нужно применить `supabase/2026-09-07_atomic_report_submission.sql` в тестовой/боевой Supabase и выполнить один контрольный отчёт авторизованным исполнителем.

## Этап 4 — приватность данных сотрудников (7 сентября 2026)

- Экран входа получает только профиль текущего пользователя через `get_my_profile`, а не читает таблицу сотрудников напрямую.
- Общий список сотрудников загружается через `list_profiles_safe`: обычному сотруднику доступны только коллеги его филиала, чужие оклады, кассовые остатки и персональные настройки прав сервер не возвращает.
- Свой оклад видит сам сотрудник; оклады команды — только администратор или роль с правом финансов; телефоны коллег — только роли, которым они нужны для управления заявками или командой.
- Отключённый сотрудник не получает список коллег. Новые RPC закрыты для анонимных запросов.
- Развёртывание разделено на два безопасных шага: сначала RPC и новая версия клиента, затем отзыв прямого `select` к `profiles` через `2026-09-07_profile_privacy_lockdown.sql`.
- После lockdown старые RLS-функции потребовали доступ к служебным полям профиля. Им возвращены только `id`, роль, активность, филиал и флажки разрешений; оклад, телефон и кассовые поля по-прежнему нельзя читать напрямую.

Проверки: **315 тестов пройдено**; линтер и `git diff --check` — без ошибок; production-сборка успешна. После публикации клиента требуется выполнить вторую lockdown-миграцию и проверить вход администратора и обычного сотрудника.

## Этап 5 — серверные проверки прав профиля (7 сентября 2026)

- Существующая production-логика `is_admin` и `kd_has_permission` сохранена без переписывания, но функции переведены в `SECURITY DEFINER` с фиксированным `search_path`.
- Проверки RLS теперь читают роль, активность и персональные разрешения на сервере, а не с правами браузера.
- Попытка отозвать прямой доступ к `role`, `is_active`, `branch_id` и `access_overrides` выявила дополнительные production-политики, исходников которых нет в репозитории: восемь разделов перестали загружаться с `permission denied for table profiles`.
- Для восстановления работы служебные поля были временно возвращены через `2026-09-07_profile_policy_compatibility_restore.sql`. Read-only инвентаризация production-схемы затем нашла все 17 прямых обращений политик к `profiles`.
- Выполнение функций закрыто для анонимных запросов и разрешено только авторизованным и сервисным ролям.
- Прямые проверки ролей заменены функцией `kd_has_role` с `SECURITY DEFINER`; условия каждой политики, включая доступ назначенного исполнителя и автора задачи, сохранены.
- После применения `2026-09-07_profile_policy_direct_dependencies.sql` у браузера снова отозваны `role`, `is_active`, `branch_id` и `access_overrides`; оставлен только `id`, необходимый для адресного обновления профиля.

Итоговая проверка: **325 тестов пройдено**; линтер и `git diff --check` — без ошибок. Миграция успешно применена в Supabase; контрольная авторизованная загрузка подтвердила работу разделов без `permission denied for table profiles`.

## Этап 6 — изоляция локальных данных сотрудников (7 сентября 2026)

- Очередь офлайн-статусов, черновик новой заявки и черновики отчётов получили отдельные ключи для каждого пользователя; смена аккаунта на общем устройстве больше не подхватывает чужую работу.
- Каждое новое офлайн-действие дополнительно содержит идентификатор владельца. Записи другого пользователя и старые записи без владельца не отправляются на сервер автоматически.
- При невозможности записать очередь в локальное хранилище интерфейс не меняет статус заявки и не сообщает ложное «сохранено офлайн».
- После успешного выхода удаляются снимок, очередь и черновики текущего пользователя, а также старые общие данные без владельца. Настройки устройства, например тема, сохраняются.
- Если есть несинхронизированные действия, выход требует отдельного подтверждения и прямо предупреждает об их удалении.

Проверки: **322 теста пройдено**; линтер и `git diff --check` — без ошибок; production-сборка успешна.

## Этап 7 — серверное закрытие финансового периода (7 сентября 2026)

- Настройка `books_closed_until` теперь проверяется самой базой, а не только интерфейсом. Прямой API, старая версия клиента и привилегированная RPC-функция не обходят закрытие.
- Защитные `BEFORE`-триггеры установлены на восемь проверенных однодатных журналов: `jobs`, `opex`, `money_moves`, `tech_expenses`, `cash_adjustments`, `inventory_adjustments`, `chemical_purchases`, `mkt_topups`.
- Проверяются старая и новая дата строки: закрытую операцию нельзя «перенести» в открытый период, изменить или удалить. Для старых строк без основной даты используется `created_at`.
- Ограничение действует для всех ролей, включая администратора. Исправление прошлого требует явно открыть период в Настройках; изменение самой настройки уже ограничено admin-RLS и попадает в `change_log`.
- Многодатные процессы — погашение долгов, оплаты продаж препаратов, документы и тендерные гарантии — намеренно не смешаны с первым этапом и требуют отдельных правил по дате каждого денежного события.
- Миграция `2026-09-07_server_period_lock.sql` успешно применена в Supabase; существующие строки она не изменяет.

Проверки: **328 тестов пройдено**; линтер и `git diff --check` — без ошибок. SQL успешно выполнен на production-схеме.

## Этап 8 — приватность фото и подписей клиентов (7 сентября 2026)

- Доступ к строкам `job_proofs` больше не открыт всем авторизованным сотрудникам. Подтверждение видит и обновляет только назначенный исполнитель либо сотрудник с правом управления заявками.
- Та же серверная проверка применяется непосредственно к объектам закрытого бакета `job-proofs`: знание или угадывание пути больше не позволяет получить временную ссылку на чужое фото или подпись.
- Первая папка каждого файла сверяется с `job.id`; повреждённый или посторонний путь безопасно отклоняется без преобразования пользовательского текста в UUID.
- Бакет при каждом повторном применении миграции принудительно остаётся непубличным.
- Дополнительные restrictive-политики не позволяют случайной будущей permissive-политике снова открыть материалы заявки всем сотрудникам.
- Проверка учитывает деактивацию аккаунта и выполняется внутри `SECURITY DEFINER` с фиксированным `search_path`; анонимный запуск функции запрещён.

Проверки: **335 тестов пройдено**; линтер, `git diff --check` и production-сборка успешны. Миграция `2026-09-07_job_proof_access_lockdown.sql` успешно применена в Supabase.

## Этап 9 — атомарные входящие оплаты (7 сентября 2026)

- Погашение долга клиента и оплата продажи препарата больше не состоят из двух независимых запросов браузера. Отметка в источнике и приход по счёту проводятся одной серверной транзакцией.
- Сумма движения берётся самой базой из заблокированной на время операции строки долга или продажи; браузер не может подменить её отдельным параметром.
- Счёт и дата обязательны. Интерфейс больше не разрешает создать отметку «оплачено» без фактического движения денег.
- Повтор точно такой же операции возвращает уже существующее движение. Конфликтующая дата, счёт или сумма отклоняются и требуют сверки вместо создания второго прихода.
- Старые открытые вкладки приложения также не могут создать новый дубль: серверный триггер сериализует параллельные вставки по паре `source/ref_id`.
- RPC разрешены только активному администратору или сотруднику с правом финансов; анонимное выполнение отозвано. Закрытие периода продолжает проверяться существующими серверными триггерами.
- Если RPC отсутствует, новая версия клиента не возвращается к прежнему небезопасному пути и прямо называет необходимую миграцию.

Проверки: **340 тестов пройдено**; линтер, `git diff --check` и production-сборка успешны. Миграция `2026-09-07_atomic_receipts.sql` успешно применена в Supabase.

## Этап 10 — атомарные расчёты и тендерные залоги (7 сентября 2026)

- Расчёт с партнёром по комплекту документов и внесение тендерного обеспечения переведены с двух независимых запросов браузера на серверные транзакции.
- Для документов сервер сам определяет направление денег по схеме `for_partner` / `via_partner` и рассчитывает сумму из зафиксированных `amount` и `percent`.
- Для тендерного залога расход всегда берётся из серверной суммы обеспечения; браузер передаёт только идентификатор, счёт и дату.
- Счёт стал обязательным. Отметку «рассчитано» или «внесено» больше нельзя создать без соответствующего движения денег.
- Точный повтор возвращает существующее движение, а конфликтующие реквизиты отклоняются для ручной сверки. Параллельная запись из старой вкладки не создаёт дубль.
- RPC проверяют активность пользователя и профильные разрешения, закрыты для анонимного запуска и продолжают подчиняться серверному закрытию периода.
- Формы показывают ошибку внутри окна и не заявляют об успехе, если серверная транзакция не завершилась.

Проверки: **345 тестов пройдено**; линтер, `git diff --check` и production-сборка успешны. Миграция `2026-09-07_atomic_settlements.sql` успешно применена в Supabase.

## Этап 11 — безопасные частичные возвраты тендерных залогов (8 сентября 2026)

- Частичный возврат обеспечения и приход денег на выбранный счёт теперь создаются одной серверной транзакцией. Половинчатая запись больше не оставляет тендер и кассу в разных состояниях.
- Сервер блокирует обеспечение на время операции и сам пересчитывает уже возвращённую сумму. Устаревший экран или параллельный запрос не позволяют вернуть больше исходного залога.
- Счёт и дата обязательны, а возврат ещё не внесённого обеспечения отклоняется.
- Каждая попытка получает устойчивый `request_id`. Повтор после обрыва связи возвращает уже созданную запись, а не дублирует возврат и приход.
- Серверный триггер дополнительно запрещает второе денежное движение для одной записи возврата.
- RPC доступна только активному администратору или сотруднику с правом редактировать тендеры либо финансы; анонимный запуск закрыт.
- Если миграция отсутствует, приложение не использует старую небезопасную последовательность и показывает точное имя SQL-файла.

Проверки: **351 тест пройден**; линтер, `git diff --check` и production-сборка успешны. Миграция `2026-09-08_atomic_guarantee_returns.sql` успешно применена в Supabase.

## Этап 12 — атомарное удаление тендерных залогов (8 сентября 2026)

- Удаление отдельного возврата и удаление всего обеспечения со всеми возвратами переведены с цепочки браузерных запросов на две серверные транзакции.
- Перед удалением сервер сверяет сумму, дату, счёт и направление каждого связанного денежного движения с исходной записью. При расхождении операция останавливается для ручной сверки.
- Денежные движения удаляются раньше исходных записей, но любая ошибка откатывает всю последовательность: касса и тендер не остаются в разных состояниях.
- Параллельные вставки из старой открытой вкладки сериализуются теми же блокировками, что и действующая защита от дублей.
- Повтор запроса после обрыва связи безопасен: уже отсутствующая запись не вызывает повторного изменения денег.
- Серверное закрытие периода продолжает действовать при удалении движения. Удаление не используется как обход закрытого месяца.
- RPC доступны только активному администратору или сотруднику с правом редактировать тендеры либо финансы; анонимный запуск закрыт.
- Если миграция отсутствует, клиент не возвращается к старому небезопасному удалению и показывает точное имя SQL-файла.

Проверки: **358 тестов пройдено**; линтер, `git diff --check` и production-сборка успешны. Миграция `2026-09-08_atomic_guarantee_deletions.sql` успешно применена в Supabase.

## Этап 13 — атомарная сдача наличных (8 сентября 2026)

- Подтверждение заявки и приход денег на выбранный счёт теперь выполняются одной серверной транзакцией. Статус «подтверждено» больше не может сохраниться без денег в кассе.
- Сумма прихода берётся сервером из заблокированной заявки, а не из данных браузера. Счёт и фактическая дата поступления обязательны.
- Повторное подтверждение после обрыва связи возвращает существующее движение и не создаёт второй приход.
- Подтверждённую заявку нельзя отклонить, а ожидающую заявку с неожиданным движением нельзя обработать без предварительной сверки.
- Отмена сотрудником также переведена на сервер: одновременно отменить и подтвердить одну заявку больше нельзя.
- Старые вкладки защищены от параллельной вставки второго движения по паре `deposit/ref_id`.
- Решение доступно только активному администратору или сотруднику с правом финансов; отменить ожидающую заявку может её автор либо финансовая роль. Анонимный запуск закрыт.
- Формы подтверждения и отклонения сохраняют окно открытым и показывают ошибку внутри него, если сервер не завершил операцию.

Проверки: **365 тестов пройдено**; линтер, `git diff --check` и production-сборка успешны. Дополнительно исправлена переносимость SQL-теста между Windows и GitHub. Миграция `2026-09-08_atomic_cash_deposits.sql` успешно применена в Supabase.
