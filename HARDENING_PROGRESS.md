# Укрепление существующей ERP

## Этап 1 — загрузка, вход и черновики (5 сентября 2026)

Изменения сделаны локально. Новые бизнес-модули, SQL-миграции и изменения финансовых формул в этот этап не входят. Публикация и работа с боевыми данными не выполнялись.

### Что исправлено

- Основной экран монтируется только после загрузки действующего профиля. Ошибка, отсутствующий профиль и неизвестная роль не открывают рабочие разделы. Предусмотрена повторная проверка. Запоздавший ответ старой сессии не подменяет новую.
- Кнопки обновления явно вызывают загрузку без события клика в аргументах.
- Ошибка одной таблицы не обнуляет её последние загруженные значения и не отменяет успешные ответы остальных источников. Старый параллельный запрос не перезаписывает более новый.
- Заявки и расход препаратов применяются вместе, только после успешной загрузки обеих таблиц. Соединение выполняется через индекс по идентификатору заявки.
- Постраничная загрузка заявок, расхода и движения денег использует стабильную сортировку. Ошибка следующей страницы и превышение защитного лимита не выдаются за полную историю. Это не транзакционный снимок базы: параллельные изменения на сервере всё ещё требуют отдельной проверки.
- Предупреждение о неполных или устаревших данных видно всем ролям; технические детали — только администратору. Время полного обновления не меняется после неудачной попытки. Зелёное сообщение об отсутствии проблем не подменяет ошибку загрузки.
- Офлайн-снимок разделён по пользователю, роли, филиалу и настройкам доступа. Старый общий снимок v4 больше не используется. Новый снимок не включает зарплату и начальные денежные остатки профилей; неполный кэш до 400 заявок явно обозначается. Несохранённые пользовательские черновики не удалялись.
- Черновик отчёта сохраняет оборудование, осмотры точек, причину и пояснение скидки, срок долга и остальные поля. Черновик новой заявки больше не зависит от таймера, который мог не сработать перед закрытием.
- Недоступность или переполнение локального хранилища показываются в форме; интерфейс не заявляет, что офлайн-черновик сохранён, если запись не удалась.
- Формы заявки и отчёта выходят из состояния отправки после исключения. Повторное нажатие, редактирование и закрытие во время отправки блокируются. Удаление черновика требует явного `true` от обработчика сохранения. Неопределённый сетевой результат сопровождается предупреждением проверить заявку перед повторной отправкой.
- Убрано дублирующее свойство `priceList` в настройках.

### Проверки

Добавлены проверки на уровне функций, React DOM и основного экрана:

- отказ и повтор загрузки, сохранение старых заявок, согласованность заявок и расхода;
- гонки запросов и сессий, ожидание профиля, отключённый и неизвестный пользователь;
- восстановление черновика, quota error, исключение при отправке, двойное нажатие;
- предупреждения исполнителю и невозможность восстановления чужого/общего офлайн-снимка.

Команды: `npm test`, `npm run lint`, `npm run build`, `git diff --check`. Добавлен `jsdom` как dev-зависимость для тестов интерфейса. Граф проекта обновлён через `graphify update .`; SQL не индексируется без `tree_sitter_sql`.

Итог проверки: **305 тестов пройдено (29 новых)**; линтер и проверка diff — без ошибок; production-сборка успешна, предупреждение о размере стартового пакета остаётся.

### Границы и следующие приоритеты

1. **Цельное сохранение отчёта.** `submitReport` всё ещё вызывает несколько RPC/записей последовательно; часть дополнительных записей может завершиться ошибкой после сохранения основной части. В том числе существующий обработчик пока способен вернуть `true` после такой частичной ошибки. Нужна серверная транзакция, идемпотентный ключ и тесты отказа между шагами. Исходники действующих `submit_report`, `save_report_extras` и Edge Function `admin-users` отсутствуют в репозитории; нельзя считать этот риск закрытым по клиентским тестам.
2. **Права и филиалы.** Клиентская проверка профиля не заменяет RLS. Проверить действующие политики/функции в тестовой базе и наличие `profiles.branch_id`, которое теперь читает вход. При недоступной проверке профиля свежий запуск закрыт, в том числе полностью офлайн; уже открытый рабочий экран сохраняет последние данные. Полноценный безопасный офлайн-вход — отдельная задача.
3. **Очередь офлайн-действий и старые черновики.** Они пока используют существующие ключи. Для смены пользователя на общем устройстве нужны отдельная миграция владельца, сохранение неотправленных данных и безопасный конфликт-резолвинг. Разделение снимка не является полной изоляцией всего localStorage.
4. **Полнота остальных источников и журналов.** У ряда таблиц остаются серверные ограничения выборки; ленивые журналы требуют обработки ошибок и повторов. Требуются сверки складских остатков, кассы, переводов и долгов на реальных контрольных данных.
5. **Зависимости.** Установка dev-зависимости сообщила о 7 уязвимостях дерева зависимостей; их состав и доступность исправлений нужно проверить отдельно. Принудительное обновление с breaking changes не выполнялось.

Проверки с настоящей авторизованной сессией и серверными отказами в тестовой Supabase-базе не выполнялись. Это завершённый первый этап укрепления, а не подтверждение готовности всей ERP на «10 из 10».

## Этап 2 — скорость запуска и документы (7 сентября 2026)

- ExcelJS и PDFMake удалены из стартового пакета. Excel загружается при выгрузке, PDFMake — при создании акта или сертификата.
- Стартовый JavaScript уменьшен с 4,01 МБ до 0,98 МБ; gzip — с 1,60 МБ до 0,27 МБ (примерно на 83%). Отложенные части: Excel 0,94 МБ и PDF 2,08 МБ.
- Первый сформированный документ теперь получает только что загруженные печать и подпись. Раньше React ещё не успевал обновить `settings`, поэтому первый документ мог создаваться без изображений.
- Повторное нажатие на Excel во время подготовки файла заблокировано, кнопка показывает состояние загрузки.
- Первый Excel после открытия приложения использует данные журналов, возвращённые самим запросом, а не ещё не обновившееся состояние React. При ошибке журнала или корзины неполный файл не скачивается как будто он полный.
- Ошибки динамической загрузки и формирования документов показываются пользователю и попадают в журнал клиентских ошибок.

## Этап 3 — цельное сохранение отчёта (7 сентября 2026)

- Отчёт, оплата, расход препаратов, обход точек, концентрация, оборудование, долг и причина скидки теперь отправляются одним пакетом в `submit_report_atomic`.
- Сервер сохраняет пакет в одной транзакции: ошибка любой части откатывает весь отчёт, поэтому частично завершённая заявка больше не считается успехом.
- Для каждой попытки создаётся `request_id`. Он хранится в локальном черновике и повторно используется после обрыва связи, поэтому принятый сервером запрос не создаётся второй раз.
- Сервер повторно проверяет, что отчёт сдаёт назначенный исполнитель, администратор или сотрудник с правом редактировать заявки. Дата обхода берётся из самой заявки, а не из данных браузера.
- Если миграция ещё не применена, интерфейс блокирует старый небезопасный путь и прямо называет нужный SQL-файл; отчёт при этом не изменяется.
- Добавлены unit- и React DOM-проверки нормализации общего пакета, UUID повтора и сохранения одного `request_id` после неопределённого результата и повторного открытия формы.

Проверки: **314 тестов пройдено**; линтер и `git diff --check` — без ошибок; production-сборка успешна. Для окончательной серверной проверки нужно применить `supabase/2026-09-07_atomic_report_submission.sql` в тестовой/боевой Supabase и выполнить один контрольный отчёт авторизованным исполнителем.

## Этап 4 — приватность данных сотрудников (7 сентября 2026)

- Экран входа получает только профиль текущего пользователя через `get_my_profile`, а не читает таблицу сотрудников напрямую.
- Общий список сотрудников загружается через `list_profiles_safe`: обычному сотруднику доступны только коллеги его филиала, чужие оклады, кассовые остатки и персональные настройки прав сервер не возвращает.
- Свой оклад видит сам сотрудник; оклады команды — только администратор или роль с правом финансов; телефоны коллег — только роли, которым они нужны для управления заявками или командой.
- Отключённый сотрудник не получает список коллег. Новые RPC закрыты для анонимных запросов.
- Развёртывание разделено на два безопасных шага: сначала RPC и новая версия клиента, затем отзыв прямого `select` к `profiles` через `2026-09-07_profile_privacy_lockdown.sql`.
- После lockdown старые RLS-функции потребовали доступ к служебным полям профиля. Им возвращены только `id`, роль, активность, филиал и флажки разрешений; оклад, телефон и кассовые поля по-прежнему нельзя читать напрямую.

Проверки: **315 тестов пройдено**; линтер и `git diff --check` — без ошибок; production-сборка успешна. После публикации клиента требуется выполнить вторую lockdown-миграцию и проверить вход администратора и обычного сотрудника.

## Этап 5 — серверные проверки прав профиля (7 сентября 2026)

- Существующая production-логика `is_admin` и `kd_has_permission` сохранена без переписывания, но функции переведены в `SECURITY DEFINER` с фиксированным `search_path`.
- Проверки RLS теперь читают роль, активность и персональные разрешения на сервере, а не с правами браузера.
- Попытка отозвать прямой доступ к `role`, `is_active`, `branch_id` и `access_overrides` выявила дополнительные production-политики, исходников которых нет в репозитории: восемь разделов перестали загружаться с `permission denied for table profiles`.
- Для восстановления работы служебные поля были временно возвращены через `2026-09-07_profile_policy_compatibility_restore.sql`. Read-only инвентаризация production-схемы затем нашла все 17 прямых обращений политик к `profiles`.
- Выполнение функций закрыто для анонимных запросов и разрешено только авторизованным и сервисным ролям.
- Прямые проверки ролей заменены функцией `kd_has_role` с `SECURITY DEFINER`; условия каждой политики, включая доступ назначенного исполнителя и автора задачи, сохранены.
- После применения `2026-09-07_profile_policy_direct_dependencies.sql` у браузера снова отозваны `role`, `is_active`, `branch_id` и `access_overrides`; оставлен только `id`, необходимый для адресного обновления профиля.

Итоговая проверка: **325 тестов пройдено**; линтер и `git diff --check` — без ошибок. Миграция успешно применена в Supabase; контрольная авторизованная загрузка подтвердила работу разделов без `permission denied for table profiles`.

## Этап 6 — изоляция локальных данных сотрудников (7 сентября 2026)

- Очередь офлайн-статусов, черновик новой заявки и черновики отчётов получили отдельные ключи для каждого пользователя; смена аккаунта на общем устройстве больше не подхватывает чужую работу.
- Каждое новое офлайн-действие дополнительно содержит идентификатор владельца. Записи другого пользователя и старые записи без владельца не отправляются на сервер автоматически.
- При невозможности записать очередь в локальное хранилище интерфейс не меняет статус заявки и не сообщает ложное «сохранено офлайн».
- После успешного выхода удаляются снимок, очередь и черновики текущего пользователя, а также старые общие данные без владельца. Настройки устройства, например тема, сохраняются.
- Если есть несинхронизированные действия, выход требует отдельного подтверждения и прямо предупреждает об их удалении.

Проверки: **322 теста пройдено**; линтер и `git diff --check` — без ошибок; production-сборка успешна.

## Этап 7 — серверное закрытие финансового периода (7 сентября 2026)

- Настройка `books_closed_until` теперь проверяется самой базой, а не только интерфейсом. Прямой API, старая версия клиента и привилегированная RPC-функция не обходят закрытие.
- Защитные `BEFORE`-триггеры установлены на восемь проверенных однодатных журналов: `jobs`, `opex`, `money_moves`, `tech_expenses`, `cash_adjustments`, `inventory_adjustments`, `chemical_purchases`, `mkt_topups`.
- Проверяются старая и новая дата строки: закрытую операцию нельзя «перенести» в открытый период, изменить или удалить. Для старых строк без основной даты используется `created_at`.
- Ограничение действует для всех ролей, включая администратора. Исправление прошлого требует явно открыть период в Настройках; изменение самой настройки уже ограничено admin-RLS и попадает в `change_log`.
- Многодатные процессы — погашение долгов, оплаты продаж препаратов, документы и тендерные гарантии — намеренно не смешаны с первым этапом и требуют отдельных правил по дате каждого денежного события.
- Миграция `2026-09-07_server_period_lock.sql` успешно применена в Supabase; существующие строки она не изменяет.

Проверки: **328 тестов пройдено**; линтер и `git diff --check` — без ошибок. SQL успешно выполнен на production-схеме.

## Этап 8 — приватность фото и подписей клиентов (7 сентября 2026)

- Доступ к строкам `job_proofs` больше не открыт всем авторизованным сотрудникам. Подтверждение видит и обновляет только назначенный исполнитель либо сотрудник с правом управления заявками.
- Та же серверная проверка применяется непосредственно к объектам закрытого бакета `job-proofs`: знание или угадывание пути больше не позволяет получить временную ссылку на чужое фото или подпись.
- Первая папка каждого файла сверяется с `job.id`; повреждённый или посторонний путь безопасно отклоняется без преобразования пользовательского текста в UUID.
- Бакет при каждом повторном применении миграции принудительно остаётся непубличным.
- Дополнительные restrictive-политики не позволяют случайной будущей permissive-политике снова открыть материалы заявки всем сотрудникам.
- Проверка учитывает деактивацию аккаунта и выполняется внутри `SECURITY DEFINER` с фиксированным `search_path`; анонимный запуск функции запрещён.

Проверки: **335 тестов пройдено**; линтер, `git diff --check` и production-сборка успешны. Миграция `2026-09-07_job_proof_access_lockdown.sql` успешно применена в Supabase.

## Этап 9 — атомарные входящие оплаты (7 сентября 2026)

- Погашение долга клиента и оплата продажи препарата больше не состоят из двух независимых запросов браузера. Отметка в источнике и приход по счёту проводятся одной серверной транзакцией.
- Сумма движения берётся самой базой из заблокированной на время операции строки долга или продажи; браузер не может подменить её отдельным параметром.
- Счёт и дата обязательны. Интерфейс больше не разрешает создать отметку «оплачено» без фактического движения денег.
- Повтор точно такой же операции возвращает уже существующее движение. Конфликтующая дата, счёт или сумма отклоняются и требуют сверки вместо создания второго прихода.
- Старые открытые вкладки приложения также не могут создать новый дубль: серверный триггер сериализует параллельные вставки по паре `source/ref_id`.
- RPC разрешены только активному администратору или сотруднику с правом финансов; анонимное выполнение отозвано. Закрытие периода продолжает проверяться существующими серверными триггерами.
- Если RPC отсутствует, новая версия клиента не возвращается к прежнему небезопасному пути и прямо называет необходимую миграцию.

Проверки: **340 тестов пройдено**; линтер, `git diff --check` и production-сборка успешны. Миграция `2026-09-07_atomic_receipts.sql` успешно применена в Supabase.

## Этап 10 — атомарные расчёты и тендерные залоги (7 сентября 2026)

- Расчёт с партнёром по комплекту документов и внесение тендерного обеспечения переведены с двух независимых запросов браузера на серверные транзакции.
- Для документов сервер сам определяет направление денег по схеме `for_partner` / `via_partner` и рассчитывает сумму из зафиксированных `amount` и `percent`.
- Для тендерного залога расход всегда берётся из серверной суммы обеспечения; браузер передаёт только идентификатор, счёт и дату.
- Счёт стал обязательным. Отметку «рассчитано» или «внесено» больше нельзя создать без соответствующего движения денег.
- Точный повтор возвращает существующее движение, а конфликтующие реквизиты отклоняются для ручной сверки. Параллельная запись из старой вкладки не создаёт дубль.
- RPC проверяют активность пользователя и профильные разрешения, закрыты для анонимного запуска и продолжают подчиняться серверному закрытию периода.
- Формы показывают ошибку внутри окна и не заявляют об успехе, если серверная транзакция не завершилась.

Проверки: **345 тестов пройдено**; линтер, `git diff --check` и production-сборка успешны. Миграция `2026-09-07_atomic_settlements.sql` успешно применена в Supabase.

## Этап 11 — безопасные частичные возвраты тендерных залогов (8 сентября 2026)

- Частичный возврат обеспечения и приход денег на выбранный счёт теперь создаются одной серверной транзакцией. Половинчатая запись больше не оставляет тендер и кассу в разных состояниях.
- Сервер блокирует обеспечение на время операции и сам пересчитывает уже возвращённую сумму. Устаревший экран или параллельный запрос не позволяют вернуть больше исходного залога.
- Счёт и дата обязательны, а возврат ещё не внесённого обеспечения отклоняется.
- Каждая попытка получает устойчивый `request_id`. Повтор после обрыва связи возвращает уже созданную запись, а не дублирует возврат и приход.
- Серверный триггер дополнительно запрещает второе денежное движение для одной записи возврата.
- RPC доступна только активному администратору или сотруднику с правом редактировать тендеры либо финансы; анонимный запуск закрыт.
- Если миграция отсутствует, приложение не использует старую небезопасную последовательность и показывает точное имя SQL-файла.

Проверки: **351 тест пройден**; линтер, `git diff --check` и production-сборка успешны. Миграция `2026-09-08_atomic_guarantee_returns.sql` успешно применена в Supabase.

## Этап 12 — атомарное удаление тендерных залогов (8 сентября 2026)

- Удаление отдельного возврата и удаление всего обеспечения со всеми возвратами переведены с цепочки браузерных запросов на две серверные транзакции.
- Перед удалением сервер сверяет сумму, дату, счёт и направление каждого связанного денежного движения с исходной записью. При расхождении операция останавливается для ручной сверки.
- Денежные движения удаляются раньше исходных записей, но любая ошибка откатывает всю последовательность: касса и тендер не остаются в разных состояниях.
- Параллельные вставки из старой открытой вкладки сериализуются теми же блокировками, что и действующая защита от дублей.
- Повтор запроса после обрыва связи безопасен: уже отсутствующая запись не вызывает повторного изменения денег.
- Серверное закрытие периода продолжает действовать при удалении движения. Удаление не используется как обход закрытого месяца.
- RPC доступны только активному администратору или сотруднику с правом редактировать тендеры либо финансы; анонимный запуск закрыт.
- Если миграция отсутствует, клиент не возвращается к старому небезопасному удалению и показывает точное имя SQL-файла.

Проверки: **358 тестов пройдено**; линтер, `git diff --check` и production-сборка успешны. Миграция `2026-09-08_atomic_guarantee_deletions.sql` успешно применена в Supabase.

## Этап 13 — атомарная сдача наличных (8 сентября 2026)

- Подтверждение заявки и приход денег на выбранный счёт теперь выполняются одной серверной транзакцией. Статус «подтверждено» больше не может сохраниться без денег в кассе.
- Сумма прихода берётся сервером из заблокированной заявки, а не из данных браузера. Счёт и фактическая дата поступления обязательны.
- Повторное подтверждение после обрыва связи возвращает существующее движение и не создаёт второй приход.
- Подтверждённую заявку нельзя отклонить, а ожидающую заявку с неожиданным движением нельзя обработать без предварительной сверки.
- Отмена сотрудником также переведена на сервер: одновременно отменить и подтвердить одну заявку больше нельзя.
- Старые вкладки защищены от параллельной вставки второго движения по паре `deposit/ref_id`.
- Решение доступно только активному администратору или сотруднику с правом финансов; отменить ожидающую заявку может её автор либо финансовая роль. Анонимный запуск закрыт.
- Формы подтверждения и отклонения сохраняют окно открытым и показывают ошибку внутри него, если сервер не завершил операцию.

Проверки: **365 тестов пройдено**; линтер, `git diff --check` и production-сборка успешны. Дополнительно исправлена переносимость SQL-теста между Windows и GitHub. Миграция `2026-09-08_atomic_cash_deposits.sql` успешно применена в Supabase.

## Этап 14 — атомарные выплаты сотрудникам (8 сентября 2026)

- Создание выплаты сотруднику и списание денег с выбранного счёта теперь выполняются одной серверной транзакцией. Состояние «выплачено, но по кассе не провелось» устранено.
- Новая выплата получает устойчивый `request_id`: повтор после обрыва связи возвращает существующую выплату и не списывает деньги второй раз.
- Старое неоплаченное начисление проводится в той же строке, без создания дубля. Его исходные сумма, тип, примечание и дата начисления больше не переписываются в момент оплаты.
- Фактическая дата выплаты хранится отдельно в `paid_at` и используется как дата кассового движения.
- Сумма движения берётся сервером из заблокированной строки выплаты. Браузер не может передать кассе другую сумму.
- Для одной выплаты разрешено только одно движение `payroll/ref_id`; существующие расхождения и дубли требуют сверки.
- Удаление выплаты и связанного движения также выполняется одной транзакцией после проверки суммы, счёта, даты и направления.
- RPC доступны только активному администратору или сотруднику с правом финансов; анонимный запуск закрыт. Серверное закрытие периода продолжает действовать.

Проверки: **372 теста пройдено**; линтер, `git diff --check` и production-сборка успешны. Миграция `2026-09-08_atomic_payroll.sql` успешно применена в Supabase.

## Этап 15 — атомарные рекламные расходы (8 сентября 2026)

- Пополнение рекламного канала и списание денег с выбранного счёта теперь создаются одной серверной транзакцией.
- Счёт стал обязательным: расход в расчёте ROI больше не может существовать отдельно от общей кассы компании.
- Новая запись получает устойчивый `request_id`; повтор после обрыва связи возвращает существующий расход и не списывает деньги второй раз.
- Сумма движения берётся сервером из заблокированной записи пополнения. Для одной записи разрешено только одно движение `marketing/ref_id`.
- Рекламные движения больше не маскируются под ручные: у них отдельный источник `marketing`, поэтому их нельзя случайно удалить в общем списке движений.
- Удаление одного пополнения сверяет сумму, счёт, дату и направление, затем удаляет расход и движение одной транзакцией.
- Удаление рекламного канала также проверяет все его пополнения и связанные движения. При старом или повреждённом расхождении удаление останавливается для ручной сверки.
- RPC доступны только активному администратору или сотруднику с правом финансов; анонимный запуск закрыт. Закрытие периода продолжает действовать.

Проверки: **378 тестов пройдено**; линтер, `git diff --check` и production-сборка успешны. Миграция `2026-09-08_atomic_marketing_spend.sql` успешно применена в Supabase.

## Этап 16 — надёжное формирование актов и сертификатов (8 сентября 2026)

- Тяжёлый модуль PDF по-прежнему не замедляет первый экран, но теперь подготавливается в фоне сразу после входа и сохраняется в памяти вкладки.
- Открытая вкладка больше не откладывает загрузку PDF-модуля до клика. Поэтому публикация новой версии сайта во время рабочего дня не ломает последующее формирование акта старой вкладкой.
- Акт и гарантийный сертификат используют один управляемый загрузчик. Одновременные клики не запускают повторную загрузку двухмегабайтного модуля.
- После временного сбоя загрузчик разрешает повторную попытку, а не сохраняет ошибку до полного закрытия браузера.
- Если вкладка всё же была открыта до этого исправления и её файл уже устарел, сотрудник получает понятную инструкцию обновить страницу и повторить действие вместо общей фразы без решения.
- Повреждённая, обрезанная или неподдерживаемая картинка печати либо подписи больше не блокирует весь документ: акт формируется без проблемного украшения, а данные заявки сохраняются в PDF.
- Технические подробности ошибки продолжают записываться в журнал сбоев, но не показываются сотруднику и не раскрывают внутреннее устройство приложения.

Проверки: **385 тестов пройдено**; линтер, `git diff --check` и production-сборка успешны. Новая миграция Supabase не требуется.

## Этап 17 — атомарные поступления по выполненным заявкам (8 сентября 2026)

- Зачёт клиентской оплаты перечислением и приход денег на выбранный счёт теперь выполняются одной серверной транзакцией.
- Счёт и фактическая дата поступления обязательны. Заявку больше нельзя пометить оплаченной без отражения денег в кассе компании.
- Сумма перечисления берётся сервером из заблокированной заявки; браузер не может провести на счёт другую сумму.
- Закрытие заявки партнёром-исполнителем также перенесено на сервер. Наша доля рассчитывается из полной суммы и процента партнёра внутри транзакции.
- Когда партнёр переводит нам нашу долю, статус заявки и движение `executor_net` создаются вместе. При QR-оплате сервер фиксирует выручку и долг партнёру одним согласованным обновлением заявки.
- Точный повтор после обрыва связи возвращает существующее движение, а конфликтующие сумма, счёт, дата или способ расчёта останавливаются для сверки.
- Старые вкладки защищены от параллельного создания второго движения `job_transfer` или `executor_net`.
- RPC доступны только активному администратору или сотруднику с правом заявок либо финансов; анонимный запуск закрыт. Закрытый финансовый период продолжает контролироваться сервером.
- Формы сохраняются открытыми и показывают понятную ошибку, если единая операция не завершилась.

Проверки: **392 теста пройдено**; линтер, `git diff --check` и production-сборка успешны. Миграция `2026-09-08_atomic_job_receipts.sql` успешно применена в Supabase.

## Этап 18 — атомарный приход препаратов (8 сентября 2026)

- Создание нового препарата теперь одновременно создаёт его первоначальную строку закупки. Начальный остаток больше не существует без истории цены и даты прихода.
- Пополнение существующего препарата обновляет общий объём и добавляет закупку одной серверной транзакцией. Половинчатое состояние «остаток вырос, история не записалась» исключено.
- Сервер блокирует карточку препарата на время пополнения и прибавляет количество к актуальному остатку. Две одновременные поставки больше не могут затереть друг друга.
- Каждая форма получает устойчивый `request_id`: повтор после обрыва связи возвращает уже созданный приход и не увеличивает склад второй раз.
- Цена, поставщик, номер партии и срок годности теперь действительно сохраняются в истории закупки. Ранее интерфейс собирал номер партии и срок, но клиент терял их перед записью.
- Сервер проверяет положительное количество, допустимую единицу измерения, неотрицательные цену и минимальный остаток, а также срок годности не раньше даты поступления.
- RPC доступны только активному администратору или сотруднику с правом склада; анонимный запуск закрыт. Закрытие периода продолжает защищать дату закупки.
- При ошибке форма остаётся открытой и показывает причину; клиент не возвращается к прежней последовательности из двух запросов.

Проверки: **398 тестов пройдено**; линтер, `git diff --check` и production-сборка успешны. Миграция `2026-09-08_atomic_stock_receipts.sql` успешно применена в Supabase.

## Этап 19 — атомарная продажа препаратов партнёрам (8 сентября 2026)

- Продажа препарата и списание с остатка сотрудника теперь выполняются одной серверной транзакцией. Продажа больше не может сохраниться без соответствующего движения препарата.
- Новая продажа получает устойчивый `request_id`: повтор после обрыва связи возвращает существующую запись и не списывает препарат второй раз.
- Сумма продажи рассчитывается на сервере из количества, единицы препарата и цены. Браузер не может записать несовпадающую итоговую сумму.
- Списание сотрудника связано с продажей отдельным идентификатором и может существовать только в одном экземпляре.
- При изменении неоплаченной продажи связанное списание также обновляется, переносится на другого сотрудника либо удаляется при смене источника на центральный склад.
- Оплаченная продажа защищена от переписывания задним числом, чтобы сумма долга, оплаты и движения денег не расходилась.
- Старые продажи со списанием, которые ещё не имеют надёжной связи, не изменяются автоматически: система останавливает операцию и просит сначала сверить остаток.
- Прямое сохранение продажи из старой вкладки закрыто. Пользователь получает безопасную ошибку вместо прежней половинчатой записи.
- Дата продажи теперь защищена серверным закрытием периода. RPC доступна только активному администратору или сотруднику с правом склада либо финансов; анонимный запуск закрыт.

Проверки: **406 тестов пройдено**; линтер, `git diff --check` и production-сборка успешны. Миграция `2026-09-08_atomic_chemical_sales.sql` успешно применена в Supabase.

## Этап 20 — атомарные расчёты с партнёрами по заявкам (8 сентября 2026)

- Выплата доли обычному партнёру, выплата партнёру-исполнителю и получение компенсации за препараты теперь проводятся через выбранный счёт и дату.
- Отметка «выплачено» или «получено» и движение денег создаются одной серверной транзакцией. Галочка больше не может измениться отдельно от кассы.
- Суммы берутся сервером из заблокированной заявки. Для совместной работы сервер повторяет расчёт доли с учётом себестоимости препаратов по истории закупок.
- Расходы партнёрам записываются как расход, компенсация от партнёра — как приход. У каждого вида расчёта отдельный источник в денежном журнале.
- Повтор после обрыва связи возвращает существующее движение и не списывает или не зачисляет деньги второй раз.
- Если старая отметка уже стоит, но движения по счёту нет, система останавливает операцию для сверки вместо создания недоказанного платежа.
- Проведённую выплату больше нельзя отменить простым снятием флага. Старые открытые вкладки также не могут обойти это правило.
- Кнопки проведения видят только сотрудники с правом финансов; сервер дополнительно требует активного пользователя с финансовым доступом.
- Счёт и фактическая дата обязательны, а закрытие финансового периода продолжает действовать на созданное движение.

Проверки: **413 тестов пройдено**; линтер, `git diff --check` и production-сборка успешны. Миграция `2026-09-08_atomic_partner_settlements.sql` успешно применена в Supabase.

## Этап 21 — компактный мобильный командный центр (8 сентября 2026)

- На телефоне большой почасовой график заменён компактным списком заявок на выбранный день. Время, адрес, вид работ и исполнитель видны сразу; полная форма открывается нажатием.
- Блок выходных на телефоне сжат: календарь занимает меньше места, а сотрудники показаны короткими цветными отметками.
- На складе появился отдельный список препаратов. По нажатию открывается только выбранный препарат; при необходимости можно показать все карточки.
- В разделе сотрудников появился быстрый выбор мастера. После выбора остаются только его препараты, выплаты и закреплённое оборудование.
- Выполненные заявки показаны короткими строками с адресом, датой, номером заявки, суммой, телефоном и брендом. Полная карточка открывается только по нажатию и сворачивается обратно.
- К существующим поиску, фильтру по сотруднику и периоду добавлен фильтр выполненных заявок по бренду.
- Компьютерная версия почасового графика сохранена без изменений.

Проверки: **418 тестов пройдено**; линтер, `git diff --check` и production-сборка успешны. Новая миграция Supabase не требуется.

## Этап 22 — атомарное превращение лида в заказ (8 сентября 2026)

- Лид, заказ и первый выезд теперь создаются одной серверной транзакцией. Состояния «заявка появилась, но лид остался открытым» и «лид закрыт без заявки» исключены.
- Каждая созданная заявка хранит ссылку на исходный лид. Для одного лида разрешена только одна заявка, а одна заявка не может быть результатом конвертации нескольких лидов.
- Одновременные клики и повтор после обрыва связи блокируются на сервере: система возвращает уже созданную заявку вместо второго заказа и второго выезда.
- Если у лида уже есть повреждённая или противоречивая связь, операция останавливается с требованием сверки, а не продолжает плодить недостоверные данные.
- Успешная финальная стадия CRM выбирается на сервере. Браузер больше не может закрыть лид отдельно от создания заказа.
- Филиал берётся из профиля менеджера или филиала по умолчанию; заказ сразу получает корневую заявку и первый визит.
- RPC доступна только активному администратору или менеджеру и закрыта для анонимного запуска.

Проверки: **424 теста пройдено**; линтер, `git diff --check` и production-сборка успешны. Миграция `2026-09-08_atomic_lead_conversion.sql` успешно применена в Supabase.

## Этап 23 — оценка на месте и точные маршруты (8 сентября 2026)

- В форме заявки появился отдельный режим «Цена после оценки на месте». В нём исходная цена не требуется, а примерный ориентир менеджер оставляет в командном комментарии.
- При сдаче обычного отчёта фактическая сумма становится результатом оценки и согласованной ценой заказа. Дата оценки фиксируется автоматически.
- Заявки на оценку полностью исключены из процента поднятия цены: сравнивать фактическую продажу с фиктивным нулём система больше не будет даже при наличии старых полей цены.
- Для оценок появилась отдельная управленческая статистика: проведено, согласилось клиентов, конверсия и сумма продаж после выезда.
- Незавершённая оценка выделяется в карточке заявки; после отчёта рядом показывается зафиксированная сумма.
- Перед созданием оценочной заявки клиент проверяет наличие новой схемы и не создаёт обычную заявку вместо оценки в режиме совместимости.
- При построении маршрута каждый адрес без населённого пункта получает уточнение «Алматы, Казахстан». Яндекс больше не ищет вторую городскую улицу в другом городе.
- Явно указанные населённые пункты — например, село Шамалган или город Каскелен — сохраняются без добавления Алматы; координаты и готовые ссылки также не изменяются.

Проверки: **434 теста пройдено**; линтер, `git diff --check` и production-сборка успешны. Миграция `2026-09-08_on_site_estimates.sql` успешно применена в Supabase.

## Этап 24 — атомарные плановые выезды по договорам (8 сентября 2026)

- Плановый заказ, его первый выезд, связь между ними и перенос следующей даты договора теперь выполняются одной серверной транзакцией.
- Каждый выезд хранит дату цикла обслуживания. Для одного договора и одной даты разрешена только одна действующая заявка.
- Сервер блокирует договор на время операции и сверяет дату, которую видел пользователь. Два одновременных клика больше не создают текущий и следующий месяц подряд.
- Точный повтор после обрыва связи возвращает уже созданную заявку, не создавая второй заказ и не передвигая календарь ещё раз.
- Старые договорные заявки получают дату цикла из даты выезда. Если на одну дату уже заведено несколько действующих заявок, миграция останавливается для сверки.
- Приостановленный договор, отрицательная стоимость и некорректная периодичность отклоняются сервером.
- Цена договора фиксируется и в заказе, и в заявке; заказ сразу получает корневой визит с типом «договорной».
- RPC доступна только активному администратору или сотруднику с правом управления заявками и закрыта для анонимного запуска.

Проверки: **440 тестов пройдено**; линтер, `git diff --check` и production-сборка успешны. Миграция `2026-09-08_atomic_contract_visits.sql` успешно применена в Supabase.

## Этап 25 — атомарный контроль качества (8 сентября 2026)

- Результат звонка, оценка, комментарий и запрос отзыва теперь сохраняются одной серверной операцией.
- Если клиенту нужен повторный выезд, результат контроля и перевод заявки в «Повторы» выполняются одной транзакцией. Состояние «контроль сохранён, а повтор не создан» исключено.
- Сервер блокирует заявку на время сохранения. Двойной клик и два одновременных запроса обновят одну проверку, а не создадут дубли.
- Для одной заявки разрешена только одна актуальная проверка качества. Если в старых данных уже есть дубли, миграция останавливается и просит сначала провести сверку.
- Результат звонка и оценка проверяются сервером. Запрос отзыва сохраняется только при положительном результате; скрытая старая галочка не уйдёт клиенту после жалобы или повтора.
- Уже начатый или завершённый повтор не откатывается при последующем редактировании контроля качества.
- Старый раздельный способ сохранения закрыт, чтобы открытая вкладка прежней версии не смогла снова создать половинчатую запись.
- RPC доступна только активному администратору или менеджеру и закрыта для анонимного запуска.

Проверки: **446 тестов пройдено**; линтер, `git diff --check` и production-сборка успешны. Миграция `2026-09-08_atomic_quality_control.sql` успешно применена в Supabase.

## Этап 26 — атомарная передача оборудования (8 сентября 2026)

- Передача оборудования теперь закрывает выдачу у прежнего сотрудника и создаёт выдачу у нового одной серверной транзакцией.
- Новая выдача хранит прямую связь с исходной. Для одной исходной выдачи разрешена только одна передача.
- Сервер блокирует исходную выдачу на время операции. Повторный запрос тому же сотруднику возвращает уже созданную выдачу, а попытка передать второй раз другому человеку останавливается.
- Передать можно только оборудование со статусом «у сотрудника», с положительным количеством и другому активному сотруднику.
- Отложенная серверная проверка не позволяет даже старой открытой версии интерфейса пометить оборудование переданным без одновременной выдачи новому владельцу.
- Сервер сверяет позицию и количество в обеих сторонах передачи, поэтому связь нельзя подменить другой единицей оборудования.
- Удаление истории выдач закрыто для обычных пользователей: ответственность за оборудование остаётся проверяемой.
- RPC доступна только активному администратору или сотруднику с правом управления командой и закрыта для анонимного запуска.

Проверки: **452 теста пройдено**; линтер, `git diff --check` и production-сборка успешны. Миграция `2026-09-08_atomic_equipment_transfers.sql` успешно применена в Supabase.

## Этап 27 — атомарное создание обычных заявок (8 сентября 2026)

- Обычная заявка, объект клиента, заказ и ссылка на первый выезд теперь создаются одной серверной транзакцией.
- Состояния «объект есть, а заявки нет», «заказ есть без выезда» и «заявка не связана с заказом» больше не возникают при обрыве связи.
- Каждая попытка создания получает устойчивый `request_id`, который хранится вместе с черновиком. Повторный клик или повтор после неизвестного сетевого результата возвращает уже созданную заявку вместо дубля.
- Сервер последовательно проверяет телефон, адрес, вид работ, площадь, гарантию, режим цены и наличие филиала до записи данных.
- Осмотр по-прежнему можно создать без цены. Для режима «оценка на месте» сервер сам очищает фиктивную исходную цену и варианты стоимости.
- Объекты объединяются по нормализованному адресу, поэтому повторное обращение по той же точке не создаёт лишний объект.
- Статус и рабочий этап заявки определяются сервером по назначенному исполнителю; автор, филиал, номер визита и тип первого визита не принимаются на доверии от браузера.
- RPC доступна только активному администратору или сотруднику с правом управления заявками и закрыта для анонимного запуска.

Проверки: **458 тестов пройдено**; линтер, `git diff --check` и production-сборка успешны. Миграция `2026-09-08_atomic_job_creation.sql` успешно применена в Supabase.

## Этап 28 — атомарные повторные и контрольные выезды (9 сентября 2026)

- Повторный гарантийный выезд и контрольный визит по заказу теперь создаются одной серверной операцией.
- Сервер блокирует исходную заявку и заказ до назначения номера визита. Два менеджера больше не смогут одновременно создать внутри заказа два выезда с одинаковым номером.
- Для пары «заказ + номер визита» действует уникальная защита базы данных.
- Для одной исходной заявки разрешён только один действующий гарантийный выезд. Повторный клик возвращает уже созданную заявку вместо дубля.
- Исходная заявка снимается с очереди повторов только в той же транзакции, в которой успешно появился гарантийный выезд.
- Контрольный визит получает устойчивый `request_id`; повтор после обрыва связи не создаёт ещё один визит.
- Тип, адрес, объект, филиал и клиентские данные нового выезда берутся сервером из исходной заявки. Номер и связи больше не рассчитываются в браузере.
- RPC доступна только активному администратору или сотруднику с правом управления заявками и закрыта для анонимного запуска.

Проверки: **464 теста пройдено**; линтер, `git diff --check` и production-сборка успешны. Миграция `2026-09-08_atomic_order_visits.sql` успешно применена в Supabase.

## Этап 29 — сезонная аналитика и рекламный план (9 сентября 2026)

- В боковом меню появился отдельный раздел «Аналитика» с двумя понятными режимами: реальные данные компании и внешняя сезонная модель.
- Реальная аналитика строится по выполненным заявкам сайта: показывает сезон по месяцам, выручку, средний чек, популярные виды работ и пик спроса. Гарантийные и контрольные выезды не выдают за новый спрос.
- Год и вид работ можно выбрать отдельно. При короткой истории интерфейс прямо предупреждает, что вывод предварительный.
- Большой Excel-файл преобразован в компактную встроенную модель: 34 направления, 12 месяцев и 39 источников. В браузере не нужно загружать или открывать Excel.
- Для каждого месяца показано, что рекламировать, какая сейчас активность, за сколько недель запускать рекламу и сколько денег направить на каждую услугу.
- Месячный бюджет берётся из уже существующей цели выручки и процента на рекламу. Общая сумма распределяется полностью, без потерянного остатка из-за округления.
- Ответ «куда вложить» строится по уже заданным месячным планам рекламных каналов; рядом видны привязанный источник, число выполненных заявок и фактический ROI.
- У каждого направления открываются сезонное объяснение, рекламный посыл, целевые объекты, региональная поправка и прямые ссылки на использованные источники.
- Исторические варианты одного вредителя объединяются в аналитике независимо от регистра и формы слова: «Постельные Клопы», «Клопы» и «клопы» дают одну строку «Постельные клопы». Составные работы сохраняются отдельной строкой, чтобы не удваивать заявку и выручку.
- Добавлен воспроизводимый импортёр: при обновлении исходного Excel встроенную сезонную модель можно пересобрать одной командой.
- Экран адаптирован для телефона: две главные кнопки складываются вертикально, таблицы прокручиваются, график и карточки остаются компактными.

Проверки: **468 тестов пройдено**; линтер, `git diff --check` и production-сборка успешны. Новая миграция Supabase не требуется.
