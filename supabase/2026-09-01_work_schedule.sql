-- =====================================================================
-- График работы сотрудника: «6/1», «5/2», «7/0».
-- Нужен, чтобы считать оклад с вычетом за пропуски сверх положенных
-- выходных. Данные об отсутствиях уже есть в tech_days_off.
--
-- Запускать в Supabase → SQL Editor. Повторный запуск безопасен.
-- =====================================================================

alter table public.profiles add column if not exists work_schedule text;

-- Проверка после запуска:
-- select full_name, salary_monthly, work_schedule from profiles where role = 'tech';
